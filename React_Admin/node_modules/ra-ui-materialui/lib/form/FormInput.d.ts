import { HtmlHTMLAttributes, ReactElement } from 'react';
import PropTypes from 'prop-types';
import { Record } from 'ra-core';
import { ClassesOverride } from '../types';
declare const useStyles: (props?: any) => globalThis.Record<"input", string>;
declare const FormInput: {
    <RecordType extends Record | Pick<Record, string | number> = Record>(props: FormInputProps<RecordType>): JSX.Element;
    propTypes: {
        classes: PropTypes.Requireable<object>;
        input: PropTypes.Requireable<PropTypes.ReactNodeLike>;
    };
    displayName: string;
};
export interface FormInputProps<RecordType extends Record | Omit<Record, 'id'> = Record> extends HtmlHTMLAttributes<HTMLDivElement> {
    basePath: string;
    classes?: ClassesOverride<typeof useStyles>;
    input: ReactElement<{
        label?: string;
        source?: string;
        id?: string;
        [key: string]: unknown;
    }>;
    margin?: 'none' | 'normal' | 'dense';
    record?: RecordType;
    resource?: string;
    variant?: 'standard' | 'outlined' | 'filled';
}
export default FormInput;
